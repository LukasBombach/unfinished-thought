import { styled } from "lib/styled";

export const Layout = styled("main", {
  fontFamily: "sans-serif",
  color: "$darkBlue",
  padding: "0 $16",
  fontSize: "$20",
  lineHeight: "$16",
  margin: "0 auto",
  maxWidth: 720,
});
